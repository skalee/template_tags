require 'spec_helper'

describe TemplateTags do
  it 'has a version number' do
    expect(TemplateTags::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
